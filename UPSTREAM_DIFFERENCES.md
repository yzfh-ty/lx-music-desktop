# 与原版的差异

本文档描述本分支（Fork）相对上游 [lyswhut/lx-music-desktop](https://github.com/lyswhut/lx-music-desktop) 的全部差异。分叉点为上游提交 `9c364b4`（更新依赖），当前增量约 **75 个文件、+11900 行**。

原版的播放、歌单、排行榜、搜索、桌面歌词、主题、数据同步、开放 API、音源导入管理等核心功能**全部保留、未做改动**。本文只记录新增与修改的部分。

## 一、新增功能

### 1. 歌单订阅下载

- 订阅音乐平台歌单（支持歌单 ID 或链接），按自定义周期检查新增歌曲（最小 10 分钟），也可随时手动立即同步
- 订阅管理：添加 / 暂停 / 恢复 / 编辑 / 删除；同一歌单不可重复订阅
- 全局音乐库去重：以「平台 + 歌曲 ID」为唯一键，同一首歌无论出现在多少歌单里只下载一次
- 任务系统：17 种任务状态、独立重试列表（手动批量重试）、失败原因持久化；程序重启后自动恢复未完成任务
- 历史记录：按关键字 / 平台 / 状态筛选，记录每次下载与上传的完整快照
- 磁盘空间保护：低于阈值自动锁定下载队列，并暂停待处理、解析中、下载中及原版下载列表中等待的订阅任务；清理磁盘后手动确认恢复

### 2. 音质自动升级

- 自定义音源可返回真实音质参数（`quality` / `bitrate` / `codec` / `sampleRate` / `bitDepth`），音源桥接层会校验并透传
- 下载前后双重校验真实音质，只有严格高于云端已确认音质才覆盖升级；可设置「达到此音质后停止升级」阈值（默认 FLAC）
- 旧版音源（只返回 URL）同样支持订阅：下载后从本地文件解析真实音质再判断

### 3. CloudDrive2 集成

- 通过 gRPC 与 [CloudDrive2](https://www.clouddrive2.com/) 通信：复制文件到挂载目录、跟踪传输任务、确认上传成功
- 支持 CloudDrive2 Docker 容器内外挂载路径映射：本机文件操作与容器内 API 挂载点分别配置，并保留普通部署的自动匹配行为
- 订阅歌曲上传确认成功后才清理本地文件，确认不了绝不删除
- 下载设置中可为**手动下载**配置三选一同步：不同步（默认）/ 上传并保留本地 / 上传后删除本地。元数据、封面和歌词处理完成后才开始上传；两种上传模式都会等待 CloudDrive2 明确确认，超时保留本地文件
- 手动下载确认上传成功后会把歌曲、实际云端路径和音质写入全局去重索引；同一首歌已在云端且音质不低于本地时自动跳过，音质更高时可沿用已有路径升级
- 订阅数据库每日自动备份到本机（保留最近 10 份），也可手动立即备份；本地备份不依赖 CloudDrive2 同步开关、连接配置或在线状态

### 4. 扫描网盘已有歌曲 + 目录一致性检查

- **扫描网盘已有歌曲**：一次性只读扫描 CloudDrive2 音乐目录，把已有歌曲登记到本地去重索引（含真实音质）。扫描完成后自动在线匹配待确认歌曲（网易云优先，歌名完全一致 + 歌手匹配 + 时长 ±5 秒才自动关联），无法自动匹配的进入人工确认列表
- **目录一致性检查**：只枚举网盘路径与本地记录对照（不读取音频），支持手动执行与定时自动检查
- 这两项都位于「设置 → 订阅设置」

### 5. 下载临时目录

- 订阅歌曲下载到独立的临时目录（可自定义，留空使用原版下载目录）
- 开启 CloudDrive2 同步时：上传确认后自动清理；关闭同步（仅本地模式）时永久保留，可直接作为本地音乐库（如 Navidrome）的目录
- 设置页提供「临时目录中的歌曲」列表，随时可见目录里有哪些文件

## 二、对原版功能的修改

| 修改点 | 影响范围 | 说明 |
| --- | --- | --- |
| 自定义音源桥接增强 | 所有音源解析 | 原版只接受音源返回 URL 字符串；现兼容返回对象（含音质参数，严格校验后透传）。只返回 URL 的旧音源完全兼容 |
| 音乐核心暴露音质详情 | 音源 URL 获取链路 | 新增 `getMusicUrlDetail`，把音源报告的真实音质参数带出来；原 `getMusicUrl` 对外行为不变 |
| 元数据写入函数 Promise 化 | MP3/FLAC 元数据写入 | 原版失败只打日志；现在可被订阅和手动 CloudDrive2 同步流程等待，并修正了不支持格式误入 FLAC 处理器的分支问题 |
| 下载数据库表结构 | download_list 表 | 新增 `subscriptionTaskId` 列，旧数据自动迁移保留；另新增 6 张订阅相关表 |
| 下载管理器订阅与手动同步分支 | 下载队列 | 订阅任务完成后移除条目并进入上传后处理、失败联动标记订阅任务；普通手动下载默认行为不变，仅在用户开启 CloudDrive2 同步时追加“元数据/歌词完成 → 上传确认 → 可选清理”流程 |
| 数据库 Worker 并发 | 主进程数据库 IPC | Comlink 每个并发请求会临时注册一个监听器；为订阅页批量初始化设置合理上限，避免超过 Node 默认值时产生误报型内存泄漏警告 |
| UI 增量 | 侧边栏 / 路由 / 设置页 | 新增「订阅」入口（受开关控制）、`/subscription` 路由、订阅设置分区；原版各页面排版未动 |
| 构建与依赖 | 构建配置 | 新增依赖与 `test` 脚本；主进程的 `music-metadata` 及其依赖由 Webpack 编译进产物，避免安装后因缺少模块无法启动 |

## 三、默认设置差异

| 设置 | 默认值 | 位置 |
| --- | --- | --- |
| 启用订阅功能 | 关闭 | 设置 → 订阅设置 |
| 下载完成后同步到 CloudDrive2（订阅歌曲） | 开启 | 设置 → 订阅设置 |
| 上传后保留本地文件（订阅歌曲） | 关闭 | 设置 → 订阅设置 |
| 同步周期 | 逐订阅设置，最小 10 分钟 | 订阅页 → 订阅管理 |
| 手动下载同步到 CloudDrive2 | 不同步 | 设置 → 下载设置 |
| 下载临时目录 | 未设置（使用原版下载目录） | 设置 → 订阅设置 |
| 达到此音质后停止升级 | FLAC | 设置 → 订阅设置 |
| 本地磁盘阈值 | 30 GB | 设置 → 订阅设置 |

其余原版已有选项（下载目录、文件名格式、最大同时下载数、封面、歌词、LRC 等）全部直接沿用原版设置页，无重复配置。

## 四、兼容性说明

- 原版 LX Music 的自定义音源**无需任何修改**即可用于订阅下载：只返回 URL 的旧音源会走「下载后本地复核」路径
- 订阅任务下载期间会临时出现在原版「下载」列表中（完成后自动移除）
- 关闭订阅功能会取消尚未完成的初始化并停止后续调度；已经开始的普通下载不会被强制中断
- 旧版本 LX Music 的数据（歌单、下载列表、配置）可正常迁移；订阅功能使用独立的数据表，首次使用自动建表
