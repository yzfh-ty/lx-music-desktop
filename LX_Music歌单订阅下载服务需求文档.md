# LX Music 歌单订阅下载服务需求文档

> 当前开发阶段只交付桌面软件内的订阅、下载、校准和 CD2 同步能力。Docker、VNC 与 noVNC 部署适配保留为后续阶段，不属于本阶段实现与验收范围。

## 1. 项目目标

基于 LX Music 源码进行功能增强式二次开发。保留原版界面、播放、歌单读取、自定义音源、歌曲下载和元数据写入能力，在同一个程序中新增：

- 订阅音乐平台歌单。
- 定时检查歌单中的新增歌曲。
- 使用用户导入的 LX 自定义音源解析歌曲地址。
- 自动下载尚未处理的歌曲。
- 防止同一歌曲重复下载和重复上传。
- 发现更高音质时自动升级云端文件。
- 通过 CloudDrive2（CD2）挂载路径自动上传到网盘。
- 通过新增的 LX Music“订阅下载”页面管理订阅、任务和历史记录。

## 2. 使用场景

用户通过原版 LX Music 界面导入并启用自定义音源，随后在新增的“订阅下载”页面添加歌单订阅。程序按照设定周期读取歌单，识别新增歌曲或可升级音质的歌曲，完成解析、下载和上传。

该产品就是一份经过二次开发的完整 LX Music，不依赖另一份原版 LX Music 实例。原版播放功能继续保留；部署到服务器时，在带 VNC/noVNC 的 Docker 桌面环境中运行，用户通过远程桌面管理程序。程序停止时不执行定时同步和下载任务；重新启动后应自动恢复未完成任务。

## 3. 总体流程

```text
订阅歌单
  → LX Music 获取歌单内容
  → 根据平台和歌曲 ID 查询本地数据库
  → 从 SQLite 读取已确认上传的云端音质，不查询云盘
  → 已达到停止升级阈值时直接结束，不请求音源
  → 已有歌曲缺少可比较的云端音质时直接跳过
  → 新歌曲或存在潜在音质提升时由自定义音源解析 URL 和实际音质
  → 音源实际音质缺失、不可比较或没有提升时直接跳过
  → 下载到本地临时目录
  → 扫描本地文件的编码、码率、采样率和位深
  → 已有云端版本且实际音质没有提升时丢弃本地临时文件
  → 内嵌歌曲元数据、封面和歌词
  → 复制到 CD2 挂载路径
  → 通过 CD2 gRPC 确认上传成功
  → 将实际音质和云端路径写入 SQLite
  → 进入 20 分钟延迟清理期
  → 更新下载与上传历史
  → 延迟到期并通过基础检查后清理本地文件
```

## 4. 功能需求

### 4.1 自定义音源

- 沿用 LX Music 现有的自定义音源导入方式。
- 自定义音源的导入、保存、选择、启用和切换方式完全沿用 LX Music 原版设置页与运行机制，不新增另一套音源管理界面或启用规则。
- 不新增主音源、备用音源、音源优先级、音源池、健康检查或熔断管理。
- 自动下载直接调用当前已启用音源解析 `musicUrl`、封面和歌词。
- 当前自定义音源不可用时，任务按照 LX Music 原有错误处理进入失败状态，由用户在原界面更换音源后手动重试。
- 更换自定义音源不能导致已经上传的歌曲被重新下载。

#### 4.1.1 原版错误处理与换源

- 保留 LX Music 对失效下载地址的重新解析能力，不新增独立的多轮指数退避重试器。
- 保留 LX Music“下载时是否使用其他来源”的原有设置。启用后，当前平台取不到 URL 时，可以按照歌名、歌手、专辑和时长搜索其他音乐平台的同一首歌。
- 上述“其他来源”指同一个已启用自定义音源脚本支持的其他音乐平台，不是多个自定义音源脚本之间的主备切换。
- 遇到 LX Music 识别的“请求过于频繁”错误时，遵从原版行为停止本次自动换源，不新增跨音源绕过机制。
- LX Music 原版单次处理流程最终返回失败后，将任务及失败原因写入“重试列表”，不创建自动重试计时器，也不自动重新排队。
- 音源解析、URL 失效、网络中断、下载失败、元数据失败、上传失败及旧版本清理失败等所有失败任务，均由用户在界面中手动选择并触发重试。
- 用户更换当前音源后，原失败任务仍保持失败状态，必须由用户手动重试，不能因为音源设置变化自动启动。
- 跨平台候选匹配不改变原订阅歌曲的唯一键；数据库同时记录实际下载所使用的平台歌曲信息，便于追踪版本差异。

### 4.2 歌单订阅

- 支持的音乐平台和歌单解析能力完全保持 LX Music 原版范围；本项目只在其现有能力上增加订阅自动下载，不额外承诺新平台。
- 支持添加、暂停、恢复和删除订阅。
- 每个订阅记录音乐平台、歌单 ID、名称和用户设置的检查周期；不提供强制默认同步周期，未设置有效周期时不启动该订阅的定时同步。
- 支持手动立即同步。
- 支持定时自动同步。
- 同一歌单不能重复订阅。
- 删除订阅不会删除已经下载或上传的歌曲，也不会删除下载历史。

### 4.3 歌曲去重

- 不通过文件名、文件大小或两个音频文件的内容进行重复判断。
- 使用 `平台 + 歌曲 ID` 生成歌曲唯一键，例如 `wy:2652820720`。
- 同一歌曲出现在多个订阅歌单时只下载和上传一次。
- 所有订阅共用一个全局音乐库，同一歌曲只保留一个物理文件，不按照歌单创建不同歌曲目录或复制多份文件。
- 数据库单独保存歌曲与各订阅歌单之间的关联，歌单关系不影响歌曲最终存储路径。
- 歌曲从歌单移除后再次加入，默认不重新下载。
- 用户可以手动清除某首歌的历史记录并重新下载。
- 不同平台或不同歌曲 ID 的内容无法保证完全去重，可选使用“歌名、歌手、时长”进行辅助提示，但默认不自动合并。
- “永久去重”不等于永远禁止再次下载：数据库记录不存在时按新歌曲处理；记录存在且 SQLite 已确认音质低于全局停止升级阈值时，才进入潜在升级流程。
- SQLite 是服务管理歌曲的云端状态索引，至少记录歌曲唯一键、已确认上传音质、实际云端路径和上传确认时间；日常去重和升级判断不得为了确认音质而读取或查询云盘文件。
- 只有 CD2 gRPC 明确确认上传成功后，才能更新 SQLite 中的“已确认上传音质”；仅下载完成、复制完成或上传中均不能提前更新。

#### 4.3.1 一次性云端校准与去重导入

- 首次启用自动订阅前必须提供一次性校准功能，用于把 CD2 音乐目录中已经存在的歌曲导入 SQLite 去重索引。
- 校准根目录、是否递归子目录以及需要包含或排除的目录由用户设置，不写死扫描层级。
- 校准过程只读扫描 CD2 挂载目录，不得删除、移动、重命名、覆盖或重新上传任何云端文件。
- 校准读取文件路径、音频标签、编码、平均码率、采样率、位深和时长，并生成可确认的现有云端音质。
- 用户已接受首次校准可能通过 CD2 产生云端文件读取或缓存下载；该读取只发生在用户主动启动的校准任务中。
- 文件名和标签只能用于生成候选匹配，最终自动关联至少需要歌名、歌手和时长形成唯一匹配；存在多个候选、字段缺失或匹配不可靠时进入人工确认列表，不能自动合并。
- 用户确认匹配后，数据库写入 `平台 + 歌曲 ID`、云端路径、校准音质、校准时间和 `calibrated` 标记。
- 无法匹配歌曲 ID 或无法读取音质的文件标记为 `calibration_unresolved`，保留在人工处理列表中；自动订阅不得据此猜测、下载或覆盖。
- 校准完成标志及结果必须持久化，程序重启和日常歌单同步不得自动重新扫描整个云盘。
- 用户可以手动重新运行校准，但每次仍为只读操作，并应允许限定目录和查看扫描进度、成功、冲突及失败数量。

### 4.4 音质升级

音质等级默认定义为：

```text
128k < 320k < FLAC < 24-bit FLAC
```

#### 4.4.1 LX Music 原生音质回退机制

- 保留 LX Music“先选择可用音质，再请求下载地址”的基本思路，但不能把它直接作为实际音质证明。
- LX Music 原生下载逻辑根据音源声明的 `qualityList` 和歌曲信息中的 `musicInfo.meta._qualitys` 选择请求档位，不会先下载文件再判断音质。
- LX Music 原生下载候选顺序为 `flac24bit → flac → wav → ape → 320k → 192k → 128k`；播放器使用的主要回退顺序为 `flac24bit → flac → 320k → 128k`。
- 当目标档位不受当前音源支持或歌曲未声明该档位时，从目标档位开始向下寻找首个被声明为可用的档位，最终可以回退到 `128k`。
- 上述判断只说明“准备请求哪个档位”，不证明音源最终返回的文件确实达到该档位。
- LX Music 现有自定义音源调用链会把原请求的 `type` 与返回 URL 组合为结果；如果接口静默返回较低音质，原版可能仍将其视为请求的音质。
- 因此，本项目只复用原生机制生成候选请求档位，不使用请求参数、`qualityList` 或 `_qualitys` 直接更新数据库中的已上传音质。

#### 4.4.2 SQLite 音质索引与本地文件复核

- SQLite 记录每首服务管理歌曲当前已经通过 CD2 确认上传的音质，后续去重和升级比较以该记录为准，不为此查询云盘。
- 数据库至少区分 `requested_quality`（请求档位）、`source_reported_quality`（音源报告值）、`file_verified_quality`（本地文件解析值）和 `cloud_quality`（已确认上传版本）。
- 已存在云端路径但 `cloud_quality` 缺失、校准失败或状态为 `calibration_unresolved` 时，直接停止本轮升级，不请求音源 URL、不下载、不比较、不覆盖，等待人工完成校准。
- 自定义音源能够返回 `{ url, quality, bitrate, codec, sampleRate, bitDepth }` 时，先使用其报告值与 `cloud_quality` 比较，明显相同或更低时可在下载前直接跳过。
- 自动订阅要求自定义音源在解析 URL 时同时返回可映射的实际音质；只返回 URL、仅回显请求档位、报告值缺失、可信度不足或不可比较时，直接记录跳过，不发起音频下载。
- 下载完成后必须在普通本地磁盘读取音频容器和编码信息，至少获取编码格式、平均码率、采样率和位深；以本地解析结果生成统一等级 `128k`、`320k`、`flac` 或 `flac24bit`。
- 音质统一映射为：无损编码且位深至少 24-bit 为 `flac24bit`；其他 FLAC、APE、WAV 无损文件为 `flac`；MP3 平均码率至少 256 kbps 为 `320k`；其他可识别的有效有损文件为 `128k`。
- 本地解析只能验证文件声明的编码技术参数，不能识别由有损音频转码得到的假无损或升频得到的假 Hi-Res；首个版本不进行频谱分析。
- 新歌曲没有数据库记录时，本地文件能够确认音质即可继续元数据写入和上传。
- 新歌曲虽然不需要与旧版本比较，但音源仍必须先报告可确认音质；报告值缺失时同样不下载。
- 已有云端版本时，只有 `file_verified_quality` 严格高于 SQLite 中的 `cloud_quality` 才允许上传升级；相同、更低、解析失败或不可比较时删除本次本地临时文件，不修改云端文件和数据库云端音质记录。
- 音质升级必须通过两次判断：下载前确认音源报告音质严格高于 SQLite `cloud_quality`，下载后再确认本地文件复核音质仍严格高于 `cloud_quality`。两次均通过后，相同扩展名直接覆盖原正式路径；扩展名变化时直接上传到新扩展名的正式路径，CD2 确认成功后删除数据库明确记录的旧扩展名路径。
- 覆盖上传失败时保留本地新文件并保持 SQLite 原 `cloud_quality` 不变，等待用户重试；由于采用直接覆盖，系统不承诺云端旧版本在覆盖失败期间仍可恢复。
- CD2 gRPC 明确确认新版本上传成功后，才把 `file_verified_quality` 写入 `cloud_quality`，同时更新云端路径和上传确认时间。
- 允许按用户设置定期或手动执行“目录结构校验”，只枚举目录和文件路径，不打开音频、不读取标签或技术参数，避免触发云端文件下载。
- 目录结构校验用于发现路径缺失、新增或移动并标记 SQLite 状态；无法识别同一路径下内容被外部替换，也不能据此改变 `cloud_quality`。
- 目录结构校验发现异常后仅提示并等待用户处理，不自动补传、覆盖或删除云端文件；需要重新确认音质时由用户主动启动一次性校准。

#### 4.4.3 停止升级音质设置

- 在“订阅下载”设置中增加全局“达到此音质后停止升级”选项，作用于全局音乐库。
- 可选值至少包括 `128k`、`320k`、`flac`、`flac24bit` 和“不提前停止”，默认值为 `flac`。
- 当 SQLite `cloud_quality` 已达到或高于该阈值时，将歌曲标记为 `quality_satisfied`，后续同步直接跳过 URL 解析、音频下载和音质复核。
- 该设置只控制已有歌曲是否继续升级，不是首次下载的质量上限。全新歌曲第一次下载不受停止升级阈值限制，按照 LX Music 原版能力请求当前可获得的尽可能高音质；第一次成功上传后才使用全局阈值判断后续是否继续升级。
- 用户提高阈值后，原本 `quality_satisfied` 但低于新阈值的歌曲重新具备升级资格；降低阈值后，已经达到新阈值的歌曲停止后续升级检查。
- 歌曲同时属于多个订阅时使用全局阈值，不因歌单不同创建多个物理版本。

#### 4.4.4 音质回退与原版换源关系

- 自动下载使用当前已启用自定义音源声明的 `qualityList` 和歌曲 `_qualitys`，调用 LX Music 原生 `getMusicType` 选择请求音质。
- 自动订阅始终从 LX Music 支持的最高档位发起请求，再由原生 `getMusicType` 根据当前音源和歌曲能力向下回退，以取得当前尽可能高的可用音质。
- 首个版本用于数据库比较的统一等级仍为 `flac24bit → flac → 320k → 128k`；WAV、APE 和 192k 如无法可靠映射，则不参与自动升级比较。
- 原生音质回退只负责确定本次请求档位；是否允许升级最终由本地文件复核结果与 SQLite `cloud_quality` 的比较决定。
- 音源报告值明确不高于 `cloud_quality` 时提前跳过；报告值缺失或不可靠时同样跳过，不能通过先下载再猜测的方式继续升级。
- 当前平台 URL 获取失败且用户开启 LX Music 原版“使用其他来源”设置时，允许调用原版跨平台匹配与换源流程；这不涉及切换自定义音源脚本。
- 跨平台候选必须保留歌名、歌手、专辑和时长匹配记录；未经可靠匹配的歌曲不能用于自动替换。
- 当前自定义音源整体失效时不自动启用另一个已导入脚本，任务进入失败状态，等待用户更换音源并手动重试。

### 4.5 下载任务

- 下载文件先保存到普通本地磁盘的临时目录，不能直接下载到 CD2 挂载路径。
- 本地下载目录、文件名格式、最大同时下载数及其他原版已有下载选项全部读取 LX Music 原版设置，不新增重复配置；本地下载目录若指向 CD2 挂载路径则禁止启动自动订阅下载并提示用户修改。
- 下载过程中使用临时扩展名，例如 `.part`。
- 所有自动发现的歌曲必须进入持久化下载队列。
- 下载并发数量完全使用 LX Music 原版设置页中的“最大同时下载数”配置，不新增另一套并发配置，也不在代码中强制固定为 1。
- 推荐在服务器部署时由用户把“最大同时下载数”设置为 `1`；如果用户主动调高，则自动订阅任务遵从该设置并允许相应数量的并发下载。
- 只有即将占用可用下载槽位的歌曲才能请求音源解析 URL，不能为整个歌单提前批量解析临时地址。
- 当前歌曲下载结束或明确失败并释放下载槽位后，等待任务按照原版下载队列规则补充空闲槽位。
- 元数据处理、CD2 复制和延迟清理使用后续任务状态；其中 `cleanup_wait` 不占用音频下载槽位，避免 20 分钟清理等待阻塞后续歌曲。
- 下载完整后先读取本地音频技术参数并完成音质复核；未通过升级条件的临时文件直接清理，不进入标签和上传流程。
- 支持沿用原版暂停和继续操作；失败任务统一进入重试列表，失效地址重新解析及任务重试只能由用户手动触发。
- 服务重启后能够恢复未完成任务。
- 同一歌曲同一时间只能有一个活动下载任务。

### 4.6 LX Music 原生请求策略

- 不新增统一 API 请求队列、固定 8 秒间隔、按域名限速器、指数退避、音源熔断或健康探测系统。
- URL、封面、歌词、歌单解析和跨平台换源请求沿用 LX Music 原版调用及错误处理方式。
- 下载并发沿用 LX Music 原版“最大同时下载数”设置；歌曲 URL 只在任务即将占用下载槽位或原地址失效时解析。
- 封面与歌词仍可按照 LX Music 原版流程并行请求，不要求与音频 URL 共用同一个串行限速队列。
- 自动订阅模块必须先完成数据库去重和音质基线判断，再把歌曲加入原版下载流程，避免已处理歌曲产生无意义的 URL 解析请求。
- 多个订阅同时到达同步时间时由订阅调度器依次启动同步，避免同一时刻批量读取大量歌单；不额外规定每个 API 请求之间的固定等待时间。
- 遇到“请求过于频繁”、鉴权失败、URL 获取失败或网络异常时，使用 LX Music 原版能够提供的错误信息和换源行为；最终失败的任务进入重试列表，不自动再次请求，等待用户手动重试。
- 本项目不承诺主动适配所有第三方 API 的独立频率规则；用户应使用符合音源服务要求的自定义音源。

### 4.7 音频元数据处理

- 必须沿用 LX Music 的完整下载后处理流程，不能仅获取音源 URL 后使用普通下载器保存文件。
- 对 MP3 和 FLAC 文件写入标题、歌手和专辑信息。
- 封面内嵌、原文歌词、翻译歌词、罗马音歌词、LX 逐字歌词及独立 LRC 文件全部使用 LX Music 原版设置页中的现有选项，不新增订阅专用配置。
- 元数据写入必须成为可等待的任务步骤。只有音频标签、封面和歌词处理完成后，文件才能进入 CD2 复制队列。
- 元数据处理失败时不能将未经处理的文件静默标记为完成，应记录错误并允许用户重试或选择忽略。
- 首个版本的元数据内嵌以 MP3、FLAC 为主要支持格式；不支持内嵌的格式需在界面中明确提示。

### 4.8 CloudDrive2 上传与延迟清理

- 在“订阅下载”设置中增加全局“下载完成后同步到 CD2”开关，默认开启。
- 开关开启时执行完整流程：本地下载、元数据处理、复制到 CD2、gRPC 上传确认和延迟清理。
- 开关关闭时仍执行歌单同步、去重、音源解析、下载、音质复核以及原版元数据、封面和歌词处理，但成品只保留在 LX Music 原版下载目录，不复制到 CD2 挂载目录、不查询 CD2 gRPC、不创建延迟清理任务。
- 仅本地下载完成的任务使用独立的 `local_completed` 状态，不能标记为 `uploaded`，其本地文件不能被 CD2 延迟清理器删除。
- 开关变化作用于此后完成元数据处理、准备进入上传步骤的任务；已经进入 `uploading` 的任务不被中途改写，已有 `local_completed` 文件也不自动补传。需要上传既有本地文件时必须由用户另行明确触发。
- CD2 挂载路径作为最终输出目录。
- 服务启动和每次上传前必须确认 CD2 路径已正确挂载且可写。
- CD2 未挂载时暂停上传，不能把文件写入挂载点下方的普通本地目录。
- 新歌曲写入正式目标路径；音质升级先在下载前比较音源报告音质，再在下载后比较本地文件复核音质，两次均确认高于 SQLite `cloud_quality` 后，才使用相同正式路径直接覆盖现有文件。
- 文件复制到挂载路径后，不能立即视为云端上传成功。
- 服务通过 CD2 gRPC API Token 查询对应传输任务，只有任务明确成功后才能进入延迟清理状态。
- CD2 明确确认上传成功后，本地成品文件继续保留 20 分钟。
- 20 分钟是上传成功后的额外恢复窗口，不从文件复制完成时间开始计算。
- 延迟期间“订阅下载”页面显示剩余清理时间，不能将任务显示为已经完全清理。
- 延迟到期后，应再次确认 CD2 挂载正常、目标路径存在且对应任务没有新的失败状态，再删除本地文件。
- 无法连接 CD2 gRPC、无法关联传输任务或无法取得明确成功状态时，不启动 20 分钟倒计时，也不删除本地文件。
- 任一检查失败时延后清理并记录原因，不能删除本地成品文件。
- 服务重启后必须从数据库恢复尚未到期的清理时间，不能因为重启立即删除文件或永久遗留文件。
- 删除 CD2 挂载路径中的文件会同步删除网盘文件，所有自动删除操作必须限制在明确记录的目标文件内。
- 扩展名升级清理旧文件时，删除目标必须来自任务开始前保存的 `old_cloud_path`，并再次确认该路径属于当前歌曲记录和配置的音乐库根目录；不得根据文件名模糊搜索后批量删除。

### 4.9 云端文件命名

云端文件名沿用 LX Music 原版下载文件名格式设置，自动订阅不新增独立命名模板。程序使用原版设置生成文件名主体，再根据本地复核的实际格式添加正确扩展名。

数据库必须同时保存歌曲唯一键、生成文件名时使用的原版格式设置快照和实际云端路径，不能仅依赖文件名识别歌曲。用户修改原版命名格式后，只影响后续新下载或升级文件，不自动批量重命名已上传文件。

音质升级根据扩展名采用两种正式路径流程：

```text
相同扩展名（例如 FLAC → 24-bit FLAC）
  → 下载前：音源报告音质严格高于 SQLite cloud_quality
  → 下载后：本地文件复核音质仍严格高于 SQLite cloud_quality
  → 直接覆盖 CD2 挂载路径中的正式同名文件
  → CD2 gRPC 明确确认上传成功
  → 更新 SQLite cloud_quality
  → 保留本地成品 20 分钟
  → 延迟到期后再次检查
  → 清理本地成品

扩展名变化（例如 MP3 → FLAC）
  → 下载前、下载后两次音质比较均通过
  → 保存 old_cloud_path
  → 直接上传到“歌名 - 歌手.flac”正式路径
  → CD2 gRPC 明确确认新 FLAC 上传成功
  → 原子更新 SQLite cloud_path 和 cloud_quality
  → 删除 old_cloud_path 指向的旧 MP3
  → 保留本地新文件 20 分钟
  → 延迟到期后清理本地新文件
```

不创建临时云端名称、不添加音质后缀，也不执行上传后的云端重命名。扩展名变化时允许新旧正式文件在新文件上传期间短暂共存；新文件未获 CD2 成功确认时不得删除旧文件或更新 SQLite。新文件成功但旧文件删除失败时，将任务标记为 `old_version_cleanup`，保留新 SQLite 记录并只重试删除旧路径，不得重新上传新文件。

### 4.10 下载和上传历史

每首歌曲至少记录：

- 音乐平台和歌曲 ID。
- 歌名、歌手、专辑和时长。
- 原生回退后的请求音质、音源报告音质、本地文件复核音质和 SQLite 已确认云端音质。
- 当前启用的自定义音源，以及原版跨平台换源实际使用的歌曲平台和歌曲 ID。
- 下载状态和上传状态。
- 元数据写入状态。
- 本地临时路径。
- CD2 云端路径。
- 音质升级前的 `old_cloud_path`、升级后的 `cloud_path` 和旧版本删除状态。
- 云端记录来源（正常上传或一次性校准）、校准状态、校准时间和人工确认人/时间。
- 当前停止升级阈值，以及歌曲是否已经达到 `quality_satisfied`。
- 文件复制完成时间和计划清理时间。
- 首次发现、下载完成和上传完成时间。
- 失败原因和重试次数。
- 最近一次音源解析失败原因。

历史数据库保存在普通本地磁盘，不直接放在 CD2/FUSE 挂载路径中。数据库应支持定期备份到云盘。

### 4.11 本地磁盘空间保护

- 只检查下载临时目录所在的本地磁盘，不检查 CD2 挂载盘或网盘剩余空间。
- 每次准备解析尚未开始的歌曲或为其启动新下载前检查本地可用空间。
- 本地可用空间低于 30 GB 时，暂停队列中所有尚未开始的歌曲下载，并停止为这些任务请求音源 URL。
- 已经处于 `downloading` 状态的歌曲不暂停、不取消，允许其继续完成下载和后处理。
- 空间不足状态具有锁定效果：即使可用空间后来恢复到 30 GB 以上，也不能自动恢复队列。
- 用户清理磁盘后必须在“订阅下载”页面手动确认并恢复下载队列。
- 暂停时保留所有排队任务、重试次数和订阅关系，不将其标记为失败。
- “订阅下载”页面显示本地磁盘可用空间、30 GB 阈值、暂停时间和手动恢复按钮。

## 5. 任务状态

```text
discovered  已发现
calibrating 正在执行一次性云端只读校准
calibration_unresolved 校准结果缺少歌曲匹配或音质，等待人工处理
pending     等待下载
disk_paused 本地空间不足，等待用户手动恢复
resolving   排队或正在请求音源解析
downloading 正在下载
downloaded  下载完成
quality_check 正在读取本地文件技术参数并比较 SQLite 云端音质
tagging     正在写入元数据、封面和歌词
uploading   正在上传
old_version_cleanup 新扩展名已上传成功，等待删除旧扩展名文件
cleanup_wait CD2 已确认上传成功，等待 20 分钟延迟清理
local_completed 本地下载与元数据处理完成，CD2 同步已关闭
uploaded    上传完成
failed      处理失败，保留在用户手动控制的重试列表
```

服务异常退出后：

- `downloading` 任务重新检查临时文件并恢复或重新下载。
- `calibrating` 任务从已保存的扫描进度继续或安全地重新读取当前文件，不能执行任何云端写操作。
- `calibration_unresolved` 记录保持等待人工处理，不能自动进入解析或下载流程。
- `disk_paused` 任务保持暂停，服务重启或空间恢复都不能自动继续，必须由用户手动恢复。
- `resolving` 任务重新等待处理，避免因服务重启重复创建相同下载任务。
- `quality_check` 任务重新读取本地文件技术参数，并以 SQLite 原 `cloud_quality` 重新比较，不能直接进入上传。
- `tagging` 任务重新执行或验证元数据写入，不能直接上传未处理文件。
- `uploading` 任务先检查 CD2 上传状态和目标路径，不能直接重复上传。
- `old_version_cleanup` 任务只验证并重试删除数据库记录的 `old_cloud_path`，不能重新上传新文件；旧路径已不存在时直接视为清理成功。
- `cleanup_wait` 任务恢复原计划清理时间；时间未到继续等待，时间已到则重新检查后再清理。
- `local_completed` 任务保持仅本地完成状态；程序重启或重新开启 CD2 同步后不得自动补传或删除本地文件。
- `uploaded` 任务保持历史记录，即使云端文件被用户移动或删除也不自动重传。
- `failed` 任务在程序重启、网络恢复、音源切换或下次订阅同步后仍保持失败，不自动重试；只有用户手动操作才能重新进入队列。

## 6. VNC 管理与订阅下载页面

程序运行在带 VNC/noVNC 的 Docker 桌面环境中。用户通过远程桌面访问完整 LX Music，并使用新增的“订阅下载”页面管理自动任务；首个版本不单独开发 Web 管理后台。

“订阅下载”页面至少包含：

### 6.1 概览

- 订阅数量。
- 待下载、下载中、上传中和失败任务数量。
- 最近一次同步时间。
- LX 自定义音源状态。
- CD2 挂载和写入状态。
- 本地下载磁盘可用空间、30 GB 阈值和空间不足锁定状态。

### 6.2 订阅管理

- 添加歌单订阅。
- 设置检查周期；所有订阅共用全局音乐库目录，自动下载始终请求当前尽可能高的可用音质。
- 设置全局“达到此音质后停止升级”阈值。
- 暂停、恢复、立即同步和删除订阅。

### 6.3 一次性校准

- 设置校准根目录、是否递归子目录以及包含或排除目录，然后启动只读扫描。
- 查看扫描进度、成功匹配、匹配冲突、音质缺失和读取失败的文件。
- 对无法唯一关联的平台歌曲进行人工确认或保持未解决状态。
- 校准页面必须明确提示该过程可能读取云盘文件数据，但不会修改云盘。
- 提供独立的目录结构校验设置，可配置手动或定时执行；该校验只列出路径，不读取音频元数据。

### 6.4 任务管理

- 查看歌曲、音质、进度、速度和当前状态。
- 查看下载队列顺序。
- 查看当前启用的自定义音源、原版跨平台换源结果和历史失败原因。
- 查看原生回退后的请求音质、音源报告音质、本地文件复核音质、SQLite 云端音质及最终下载或跳过理由。
- 查看元数据处理状态和本地文件延迟清理倒计时。
- 查看跨扩展名升级的新旧云端路径及旧版本清理状态，并允许手动重试旧版本清理。
- 暂停和继续任务。
- 提供独立重试列表，支持用户手动选择单个、多个或全部失败任务后执行重试；系统不得自动触发列表中的任务。
- 查看明确的失败原因。

### 6.5 历史记录

- 按歌曲、歌手、平台、音质和状态筛选。
- 查看音源报告音质、本地文件复核音质、SQLite 已确认云端音质和云端路径。
- 手动允许重新下载或升级。
- 删除历史记录时不能自动删除云端文件，除非用户单独明确选择。

## 7. 部署要求

- 使用包含图形桌面、VNC/noVNC 和 Electron 运行依赖的 Docker 镜像运行二开版 LX Music。
- 将 LX Music 配置、订阅数据库和下载临时目录分别映射到持久化卷。
- 将 CD2 音乐目录按读写方式挂载到容器。
- 容器使用的 UID/GID 或 root 用户必须拥有目标目录写权限。
- CD2 应先于下载服务启动并完成挂载。
- 保留原版 LX Music 界面和播放功能；服务器部署主要通过 VNC/noVNC 使用，自动订阅任务不主动触发播放。
- SQLite 数据、配置和日志使用独立的本地持久化目录。
- 只向容器暴露需要的 CD2 音乐目录，不能挂载整个网盘根目录。
- VNC/noVNC 必须设置身份验证，建议只通过内网、VPN或带鉴权的反向代理访问，不应直接裸露到互联网。

## 8. 非目标范围

首个版本暂不包含：

- 跨平台歌曲内容级精确去重。
- 根据音频波形判断两首歌曲是否相同。
- 自动删除用户在网盘中手动创建的文件。
- 依赖另一个外部原版 LX Music 实例提供解析或下载能力。
- 公开音乐分享、在线播放或多人账户系统。
- 绕过音乐平台权限、版权或访问限制。

## 9. 验收标准

满足以下条件即认为首个版本可用：

1. 可以导入 LX 自定义音源并成功解析歌曲地址。
2. 可以添加一个歌单订阅并手动或定时同步。
3. 首次同步能够下载歌单中的未处理歌曲。
4. 自动订阅任务遵守 LX Music 原版“最大同时下载数”设置；当设置为 `1` 时，任意时刻最多只有一个活动歌曲下载任务，其他歌曲按顺序等待。
5. URL、封面、歌词及歌单请求沿用 LX Music 原版请求和错误处理方式，不增加固定 8 秒间隔或独立限速器。
6. 下载地址失效时可以在用户手动重试时调用 LX Music 原有重新解析逻辑；任一处理步骤最终失败后任务进入重试列表，在程序重启、网络恢复、音源切换或再次同步时均不会自动重试。
7. 自定义音源的导入、保存、选择、启用和切换完全沿用 LX Music 原版机制；当前音源失效时不会自动切换到另一个自定义音源脚本。
8. 开启 LX Music 原版“使用其他来源”后，当前平台取不到 URL 时可以使用可靠匹配的其他平台歌曲继续下载。
9. 再次同步时先读取 SQLite；`cloud_quality` 已达到全局停止升级阈值的歌曲不会重复下载，也不会为其请求解析地址。
10. 同一歌曲出现在两个订阅中时只上传一次。
11. 系统沿用 LX Music 的 `qualityList`、`_qualitys` 和 `getMusicType` 选择请求档位；音源报告音质缺失或不可比较时不下载。下载完成后从本地文件读取编码、码率、采样率和位深，并且只有本地复核音质高于 SQLite `cloud_quality` 时才上传升级。CD2 确认成功后更新 SQLite，后续比较不查询云盘。
12. MP3、FLAC 下载完成后能够写入标题、歌手、专辑，并按设置内嵌封面和歌词。
13. 元数据处理完成前不会复制到 CD2。
14. CD2 未挂载时不会误写普通本地目录。
15. CD2 gRPC 明确确认上传成功后才开始 20 分钟保留期；无法确认、检查失败或时间未到时不会删除本地文件。
16. 音质升级在下载前、下载后两次确认提高：扩展名相同时直接覆盖正式路径；扩展名变化时直接上传新扩展名正式路径，CD2 确认成功后更新 SQLite 并删除明确记录的旧路径。全程不使用临时云端名称或重命名，删除失败时只重试旧路径清理，不重复上传新文件。
17. 程序或容器重启后能够恢复下载队列、未完成任务和延迟清理计时。
18. 本地空间低于 30 GB 时不再启动新下载，但正在下载的歌曲继续完成；空间恢复后必须由用户手动恢复队列。
19. 用户能够通过 VNC/noVNC 访问完整 LX Music，并在“订阅下载”页面查看订阅、任务、历史、原版跨平台换源结果、磁盘锁定状态和 CD2 状态。
20. 首次启用时可以对指定 CD2 音乐目录执行一次性只读校准；唯一匹配且音质可读的文件写入 SQLite，冲突或缺失记录进入人工处理，校准过程不会修改云盘。
21. 已存在云端文件但 SQLite 云端音质缺失或校准未解决时，不请求音源、不下载、不比较、不覆盖。
22. 用户可以设置全局停止升级音质；SQLite 已确认音质达到阈值后，后续同步不会再为该歌曲请求 URL 或检查升级。
23. 全局停止升级音质默认是 `flac`，但全新歌曲第一次下载不受该阈值限制，并请求 LX Music 当前能够获得的尽可能高音质。
24. 用户可以设置目录结构校验范围和是否递归；日常校验只枚举路径，不打开音频文件，不据此修改 `cloud_quality`。
25. 每个订阅的同步周期由用户设置；下载目录、文件名格式、最大同时下载数、封面、歌词和 LRC 等原版已有选项全部读取原版设置页，不存在订阅专用重复配置。
26. 重试列表中的音源解析、下载、元数据、上传和旧版本清理失败任务，只能由用户手动选择后重新执行，系统不执行任何任务级自动重试。

## 10. 新增设置边界

仅为原版不存在的能力新增以下设置：

- 每个订阅的同步周期。
- 全局停止升级音质阈值，默认 `flac`。
- CD2 最终音乐库根目录和 gRPC 连接信息。
- 下载完成后是否同步到 CD2 的全局开关，默认开启。
- 一次性校准的目录范围、递归和排除规则。
- 日常目录结构校验的范围及执行周期。

下载目录、文件名格式、最大同时下载数、封面、歌词、独立 LRC 等原版已经提供的选项全部直接读取原版设置页，不新增同类配置。
